{{> elements/header}}
<div class="container">
    <div class="row jumbotron">
        <div class="col-md-12">
        <h1>{{contents.username}}</h1>    
        <h3>{{contents.name}}</h3>
        <h3>{{contents.address}}</h3>
        <h3>{{contents.contact}}</h3>
        </div>
    </div>
</div>